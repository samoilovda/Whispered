[pytest]
# Only collect the real unit-test suite. The root-level test_whisper.py is a
# manual introspection script (requires pywhispercpp) and is intentionally
# excluded.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
