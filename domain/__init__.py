"""Qt-free domain types shared across engines, UI, and the Live subsystem.

Nothing in this package may import PyQt6, ``ui``, or ``core.live`` — see
``tests/test_domain_transcription.py``, which enforces that statically.
"""
